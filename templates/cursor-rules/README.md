# Cursor rules templates

Copy into your Next.js host app:

```text
.cursor/rules/matia-strategist.mdc   ← from repo root .cursor/rules/
```

Then enable the rule in Cursor when working on SEO/GEO tasks.
